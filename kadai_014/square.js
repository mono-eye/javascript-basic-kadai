/*================================
 square.js
================================*/

// アロー関数値を二乗した計算結果
const double = (num) => {
  //引数で渡した値を二乗した計算結果を
  //戻り値として返す関数を記述
  return num ** 2;
 }
 
// 関数の戻り値を出力する
console.log(double(10));