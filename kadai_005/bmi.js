/*================================
 bmi.js
================================*/

// 変数の宣言
bodyWeight = 68;
height = 1.7;

// 定数の値の代入bmiが～～計算
const bmi = (bodyWeight / (height * height));

// コンソールへの出力するのはbmi
console.log(bmi);